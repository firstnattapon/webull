# Deploy with Python 3.13 (pinned in .python-version) — the Cloud Buildpacks builder
# only ships Python 3.13+, which requires numpy 2.x. Webull SDK 2.x supports 3.13.
functions-framework==3.10.2
flask==3.1.3
google-cloud-firestore==2.11.1
numpy==2.3.1
pytz==2025.2
webull-openapi-python-sdk==2.0.12
